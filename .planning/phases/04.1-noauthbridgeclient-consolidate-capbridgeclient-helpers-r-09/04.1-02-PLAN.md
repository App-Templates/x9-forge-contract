---
phase: "4.1"
plan: "04.1-02"
title: "Forge — collapse capBridgeClient to NoAuthBridgeClient wrapper"
wave: 2
depends_on: ["04.1-01"]
requirements_addressed: [R-09]
files_modified:
  - ../forge-v2/services/factory/src/services/x9.client.ts
  - ../forge-v2/services/factory/tests/x9-client.bridge.test.ts
autonomous: true
---

# Plan 04.1-02: Forge — Collapse capBridgeClient

## Goal

Replace the local `capBridgeClient` implementation in `forge-v2/services/factory/src/services/x9.client.ts:32-72` with a thin wrapper around `createBridgeClient<'none'>()` from the bridge (added in Plan 04.1-01). Eliminates ~40 lines of duplicated HTTP plumbing. Preserves the exact exported signature so call sites in `deploy.machine.ts:283`, `x9.client.ts:182`, `x9.client.ts:209`, and the test suite need zero changes.

**Chirurgico:** Body of `capBridgeClient` only. Do NOT change its exported signature. Do NOT touch `X9Client` class. Do NOT modify the call sites.

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-4.1-04 | Wire-shape regression after wrapper migration (manifest/env-schema parse drift) | MEDIUM | Bridge `NoAuthBridgeClient.cap*` methods use the SAME `CapManifestResponseSchema` / `CapEnvSchemaResponseSchema` the local helper used — same Zod source of truth, zero shape change. Existing 12 factory tests + 2 capBridgeClient tests pass = wire shape preserved. |
| T-4.1-05 | New `capHealth` exposed but unused in Forge → dead exposure | LOW | Don't expose `capHealth` from the wrapper unless `deploy.machine.ts` already calls a `/health` endpoint. Wrapper exports only `capManifest` + `capEnvSchema` (the 2 methods the helper currently exposes). |
</threat_model>

## Tasks

<task id="04.1-02-01">
<title>Replace capBridgeClient body with createBridgeClient&lt;'none'&gt; wrapper</title>
<read_first>
- ../forge-v2/services/factory/src/services/x9.client.ts (full — focus L32-72 + import block at top)
- ../forge-v2/services/factory/tests/x9-client.bridge.test.ts:192-209 (existing capBridgeClient tests — these must pass without modification)
- bridge `src/http/bridge-client.ts` post-04.1-01 (NoAuthBridgeClient interface signature)
</read_first>
<actions>
1. In `forge-v2/services/factory/src/services/x9.client.ts`:
   - Update the `createBridgeClient` import to also pull the `NoAuthBridgeClient` type if needed for return annotation
   - Remove unused imports that were only for the local helper (`CapManifestResponseSchema`, `CapEnvSchemaResponseSchema`, `BridgeErrorResponse`, `BridgeHttpError`) IF they are no longer referenced elsewhere in the file. If `BridgeHttpError` is still used by `mapError` etc., keep it.
   - Replace `capBridgeClient` body (L32-72) with:
     ```ts
     export function capBridgeClient(svc: string, port: number): {
       readonly baseUrl: string;
       capManifest(signal?: AbortSignal): Promise<X9CapabilityManifest>;
       capEnvSchema(signal?: AbortSignal): Promise<X9EnvSchemaDoc>;
     } {
       const baseUrl = `http://${svc}:${port}`;
       const client = createBridgeClient<'none'>({ baseUrl, auth: {} });
       return {
         baseUrl,
         capManifest: (signal) => client.capManifest(signal) as Promise<X9CapabilityManifest>,
         capEnvSchema: (signal) => client.capEnvSchema(signal) as Promise<X9EnvSchemaDoc>,
       };
     }
     ```
     **Note:** Resolved during planning. `NoAuthBridgeClient.capManifest()` takes only `signal` (no `cap` arg). Bridge contract path strings were corrected from `/:cap/manifest` to `/manifest` in 04.1-01-01. Each cap service mounts manifest/env-schema/health at root; cap identity is implicit in `baseUrl` host.
2. Keep the same exported signature so call sites at `deploy.machine.ts:283`, `x9.client.ts:182`, `x9.client.ts:209`, and `tests/x9-client.bridge.test.ts:196,205` work without modification.
3. The `as` casts to `X9CapabilityManifest`/`X9EnvSchemaDoc` preserve the existing type assertion pattern from the local helper — same wire shape, same alias type.
</actions>
<verify>
- `pnpm typecheck` in forge-v2 clean
- `pnpm -r --filter "./services/factory" test` → all 74 tests still green (12 X9Client + 2 capBridgeClient + 60 baseline)
- If any test fails: STOP. The wrapper signature must be identical to the original; a failure means behavioral regression — report and revert.
</verify>
<commit>
`refactor(factory): collapse capBridgeClient to createBridgeClient<'none'> wrapper (04.1-02-01)`
</commit>
</task>

<task id="04.1-02-02">
<title>Verify call sites + tests work without modification (smoke + cleanup)</title>
<read_first>
- ../forge-v2/services/factory/src/services/deploy.machine.ts:279-285 (capBridgeClient call site)
- ../forge-v2/services/factory/src/services/x9.client.ts:180-215 (internal call sites at L182, L209)
- ../forge-v2/services/factory/tests/x9-client.bridge.test.ts:192-220 (existing tests)
</read_first>
<actions>
1. Run full factory test suite — confirm all 74 tests pass without modification
2. If any test fails: STOP. Do not "fix" the test. The wrapper signature must be identical to the original; if a test caught a behavioral regression, the wrapper has a defect. Report and revert.
3. If all tests pass: trim any newly dead imports in `x9.client.ts` (e.g. if `CapManifestResponseSchema` is no longer referenced after the wrapper migration). 1-line `imports` cleanup commit.
4. Update the comment block at L18-31 of `x9.client.ts` to remove the "When the bridge adds `auth: 'none'` support... this helper collapses to a thin wrapper" forward-reference (it's now done).
</actions>
<verify>
- `pnpm -r --filter "./services/factory" test` → 74 green
- `pnpm typecheck` in forge-v2 clean
- `git diff` shows ONLY `x9.client.ts` touched (no test file changes)
</verify>
<commit>
`chore(factory): trim dead imports + outdated comment after capBridgeClient migration (04.1-02-02)`
</commit>
</task>

## Success criteria

- [ ] `capBridgeClient` body is now ≤10 lines (delegating to `createBridgeClient<'none'>()`)
- [ ] All 74 forge-v2 factory tests pass without modification
- [ ] Zero changes to `deploy.machine.ts` or `X9Client` class
- [ ] Zero changes to test file (signature preserved)
- [ ] Net deletion in factory: at least −30 lines

## Out of scope

- Removing the `capBridgeClient` export entirely (callers still use it as a 2-arg constructor; keep until a future refactor migrates them to direct `createBridgeClient<'none'>` calls)
- Touching the X9 voice/glasses migrations from 04-03 (they use `createBridgeClient<'secret'>` / `<'token'>` — already correct)
- Forge `voice` service: no capBridgeClient there
- Adding `capHealth` to the wrapper (deploy.machine doesn't call it)
