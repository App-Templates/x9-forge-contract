---
status: all_fixed
phase: "04.1"
fix_scope: critical_warning
findings_in_scope: 3
fixed: 3
skipped: 0
iteration: 1
fixed_at: 2026-04-15
---

# Phase 04.1 Code Review — Fix Report (iteration 1)

Scope: all 3 **warning**-severity findings from `04.1-REVIEW.md`. No
critical findings were raised. The 5 **info** findings are intentionally
out of scope (IN-01..IN-05).

All fixes are docs-only; no code paths changed. `pnpm build` stayed clean
and the full suite stayed at 239/239 green after each atomic commit.

---

## WR-01 — Stale `/:cap/*` docstring in `AuthNone`
- **Commit:** `03e1be6`
- **Files touched:** `src/auth/auth-headers.ts`
- **Description:** Rewrote the `AuthNone` JSDoc block (lines 37–40) from
  "GET /:cap/manifest, GET /:cap/env-schema, GET /:cap/health" to the
  root-mounted form "`GET /manifest`, `GET /env-schema`, `GET /health`"
  with an explicit note that capability identity is conveyed by the
  caller's baseUrl, not a path prefix.

## WR-02 — Stale `/:cap/*` docstrings in capability schema files
- **Commit:** `6813c1f`
- **Files touched:**
  - `src/capability/health-status.ts` (line 4)
  - `src/capability/env-schema.ts` (lines 4 and 21)
  - `src/capability/capability-manifest.ts` (line 5)
  - `src/capability/capability-registry-entry.ts` (`@see` on line 22)
  - `src/capability/capability-tool.ts` (`@see` on lines 9–10)
- **Description:** Mechanical rewrite of each docstring listed in the
  review's File: field. `/:cap/manifest` → `/manifest`,
  `/:cap/env-schema` → `/env-schema`, `/:cap/health` → `/health`, and the
  `@see` on `capability-tool.ts:10` referencing `POST /:cap/call/:tool`
  was normalised to `POST /call/:tool` to match the same convention. The
  explanatory "capability identity is conveyed by the caller's baseUrl,
  not a path prefix" note was added to the three primary schema files
  (`health-status`, `env-schema`, `capability-manifest`). `tool-call.ts`
  and other files not listed in the review's File: field were left
  untouched to respect the "do not auto-expand scope" boundary.

## WR-03 — Stale `/:cap/*` paths in README
- **Commit:** `66ace08`
- **Files touched:** `README.md` (line 49 only)
- **Description:** Replaced the single bullet listing
  `/:cap/manifest`, `/:cap/env-schema`, `/:cap/health` with
  `/manifest`, `/env-schema`, `/health` and appended a parenthetical
  noting that capability identity is implicit in the caller's `baseUrl`
  (Docker hostname/port), not a path prefix. Only the one line changed —
  surrounding README structure was left alone.

---

## Verification

Per-fix: `pnpm build` clean, `pnpm test` 239/239 green. Final state
confirmed after WR-03 commit.

## Out of scope (info findings deferred)

- IN-01 fragile `mockFetchOnce` (fresh Response per call)
- IN-02 missing negative fixtures for cap endpoint schemas
- IN-03 `BridgeClient` default generic collapsing to `never`
- IN-04 runtime auth discriminator defence-in-depth
- IN-05 loose `RequestInit.headers` narrowing in tests

These remain for a future info-severity sweep and were explicitly
excluded from this iteration's `fix_scope: critical_warning`.
