# Phase 00-01: Bridge Package Scaffolding

**Phase:** 00 — Prerequisites + Bridge Foundation
**Plan:** 00-01
**Created:** 2026-04-14
**Status:** Ready for execution
**Depends on:** nothing (primo plan della phase, indipendente)

---

## Objective

Creare lo scaffolding completo del repo `x9-forge-contract-bridge`: package.json (con `exports` sub-path, `prepare` script, `files` field), tsconfig strict con `exactOptionalPropertyTypes`, src layout con placeholder dir per ogni dominio (capability/agent/http/auth/vault/model-router), vitest setup, README skeleton con le 3 sezioni obbligatorie OBS-01..03, e GitHub Actions CI minimale.

Al termine di questo plan: `pnpm install && pnpm build && pnpm test` deve essere verde. Il bridge non contiene contratti reali (quello inizia in Phase 1), ma ogni infrastruttura necessaria per aggiungere contratti in futuro e pronta.

**Fuori scope 00-01:**
- Nessun contratto Zod reale (solo un `DummyProbe` placeholder usato in 00-04 per il test dev-loop)
- Nessuna modifica a X9 o Forge
- Nessun typed HTTP client

---

## Requirements coperti

- **BRDG-01**: distribuzione via `git+https` URL con `prepare` build script (scaffolding predispone, non installa ancora nei consumer)
- **BRDG-02**: sub-path exports (`/capability`, `/agent`, `/http`, `/auth`, `/vault`, `/model-router`)
- **BRDG-03**: tsconfig strict + `exactOptionalPropertyTypes` + `noUncheckedIndexedAccess` + target ES2023 + NodeNext
- **BRDG-04**: Zod v4 come devDependency + peerDependency; `z.infer` pattern documentato nel README
- **BRDG-05**: `exports` + `files` field in package.json
- **OBS-01**: README con tabella contratti → file (placeholder vuoto in Phase 0, popolato incrementalmente)
- **OBS-02**: README con sezione "How to add a new contract"
- **OBS-03**: README con policy di breaking change

BRDG-06 (dev loop `pnpm.overrides`) e verificato in 00-04.

---

## Dependencies

**Dipendenze upstream:** nessuna. 00-01 puo partire in parallelo a 00-02.

**Bloccante downstream:** 00-04 richiede bridge buildato (quindi 00-01 completato) per testare il dev-loop.

---

## Task breakdown

### Task 00-01.1: Inizializzare package.json e devDependencies

**Obiettivo singolo:** scaffolding package.json completo con tutte le field richieste dalle decisioni CONTEXT.md.

**File toccati (assoluti):**
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/package.json` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/.gitignore` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/.nvmrc` (creato)

**Step concreti:**
1. `cd /Users/admintemp/Downloads/Claude/x9-forge-contract-bridge`
2. Verificare che `.planning/` esiste ma `src/`, `tests/`, `dist/` NON esistano (`ls` dovrebbe mostrare solo `.planning/`, `README.md`, `.git/`)
3. Creare `package.json` con esattamente la shape di CONTEXT.md:
   ```json
   {
     "name": "@x9-forge/contracts",
     "version": "0.0.0",
     "private": false,
     "publishConfig": { "access": "restricted" },
     "type": "module",
     "exports": {
       ".": "./dist/index.js",
       "./capability": "./dist/capability/index.js",
       "./agent": "./dist/agent/index.js",
       "./http": "./dist/http/index.js",
       "./auth": "./dist/auth/index.js",
       "./vault": "./dist/vault/index.js",
       "./model-router": "./dist/model-router/index.js"
     },
     "files": ["dist", "README.md"],
     "scripts": {
       "build": "tsc -b",
       "test": "vitest run",
       "test:watch": "vitest",
       "typecheck": "tsc --noEmit",
       "lint": "eslint src tests",
       "prepare": "pnpm build"
     },
     "peerDependencies": {
       "zod": "^4.0.0"
     },
     "devDependencies": {
       "zod": "^4.3.6",
       "typescript": "^6.0.2",
       "vitest": "^3.0.0",
       "@types/node": "^22.0.0",
       "eslint": "^9.39.4",
       "@typescript-eslint/eslint-plugin": "^8.57.2",
       "@typescript-eslint/parser": "^8.57.2"
     },
     "engines": {
       "node": ">=22.0.0",
       "pnpm": ">=9.0.0"
     }
   }
   ```
   **Nota:** `private: false` + `publishConfig.access: restricted` sono intenzionali (allineati STACK.md §10). Permettono publish privato in futuro senza leak accidentale pubblico.

   **Nota pnpm engine:** `>=9.0.0` (NON `>=10.0.0`) perche X9 usa `pnpm@9.15.9` (`agent-x9/package.json:5`) e Forge richiede `pnpm>=9` (`forge-v2/package.json:15`). Forzare `>=10` romperebbe il setup dei consumer. Upgrade a pnpm 10 nei consumer e fuori scope Phase 0.
4. Creare `.gitignore` con almeno:
   ```
   node_modules/
   dist/
   coverage/
   *.log
   .DS_Store
   .vitest-cache/
   ```
5. Creare `.nvmrc` con contenuto `22` (allineato engines.node, allinea con X9 che usa Node 22 — da `/Users/admintemp/Downloads/Claude/agent-x9/package.json:11-14`).
6. `pnpm install` → verifica lockfile creato, zero errori peer-deps.

**Success criteria (binario):**
- [ ] `pnpm install` termina con exit code 0
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/pnpm-lock.yaml` esiste
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/node_modules/zod` ha versione `^4.x` (verifica: `cat node_modules/zod/package.json | grep version`)
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/node_modules/typescript` ha versione `^6.0.2`

**Rollback:** `rm -rf package.json pnpm-lock.yaml node_modules .gitignore .nvmrc` → torna a stato pulito.

**Commit atomico:** `chore(bridge): scaffold package.json + engines + devDependencies`

---

### Task 00-01.2: tsconfig strict + src layout placeholder

**Obiettivo singolo:** creare `tsconfig.json` strict e la struttura di directory src/ con placeholder per ogni dominio (capability, agent, http, auth, vault, model-router).

**File toccati (assoluti):**
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/tsconfig.json` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/capability/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/agent/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/http/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/auth/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/vault/index.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/src/model-router/index.ts` (creato)

**Step concreti:**
1. Creare `tsconfig.json`:
   ```json
   {
     "compilerOptions": {
       "target": "ES2023",
       "module": "NodeNext",
       "moduleResolution": "NodeNext",
       "lib": ["ES2023"],
       "outDir": "./dist",
       "rootDir": "./src",
       "declaration": true,
       "declarationMap": true,
       "sourceMap": true,
       "composite": true,
       "strict": true,
       "exactOptionalPropertyTypes": true,
       "noUncheckedIndexedAccess": true,
       "noImplicitOverride": true,
       "noImplicitReturns": true,
       "noFallthroughCasesInSwitch": true,
       "esModuleInterop": false,
       "skipLibCheck": true,
       "resolveJsonModule": true,
       "forceConsistentCasingInFileNames": true
     },
     "include": ["src/**/*"],
     "exclude": ["node_modules", "dist", "tests"]
   }
   ```
   **Razionale flag:** allineato a `/Users/admintemp/Downloads/Claude/agent-x9/tsconfig.base.json:13` (che ha `exactOptionalPropertyTypes: true`). Pitfall P-14 (asimmetria Forge/X9) previene:  il bridge eredita lo strict di X9.
2. Creare `src/index.ts` (barrel minimale):
   ```typescript
   /**
    * @x9-forge/contracts — single source of truth per i contratti cross-repo
    * tra agent-x9 (runtime) e forge-v2 (control plane).
    *
    * Non importare direttamente da qui nei consumer: usare sub-path export
    * (es. `import { ... } from '@x9-forge/contracts/capability'`).
    *
    * @see README.md
    */
   export {};
   ```
3. Per ciascuno dei 6 sub-domini (capability, agent, http, auth, vault, model-router), creare `src/<dominio>/index.ts` con contenuto:
   ```typescript
   /**
    * <Dominio>: placeholder Phase 0. I contratti reali arrivano in Phase <N>.
    * @see .planning/ROADMAP.md
    */
   export {};
   ```
4. `pnpm build` → verifica `dist/` prodotto con 7 file `index.js` + relativi `.d.ts`.
5. `pnpm typecheck` → zero errori TS.

**Success criteria (binario):**
- [ ] `pnpm build` termina con exit code 0
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/dist/index.js` esiste
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/dist/capability/index.js` esiste (e analoghi per gli altri 5 sub-domini)
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/dist/index.d.ts` esiste con declaration map
- [ ] `pnpm typecheck` exit code 0
- [ ] `node -e "import('@x9-forge/contracts').then(m => console.log(Object.keys(m)))"` eseguito dal root del bridge non crasha (stamp `[]`)

**Rollback:** `rm -rf src/ dist/ tsconfig.json` → stato pre-task.

**Commit atomico:** `chore(bridge): tsconfig strict + src placeholder layout (6 sub-domains)`

---

### Task 00-01.3: Vitest setup + dummy smoke test

**Obiettivo singolo:** configurare vitest e scrivere un test smoke che conferma che il setup build+test funziona. Non test di contratti reali (quello in Phase 1).

**File toccati (assoluti):**
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/vitest.config.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/tests/setup.ts` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/tests/smoke.test.ts` (creato)

**Step concreti:**
1. Creare `vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';

   export default defineConfig({
     test: {
       include: ['tests/**/*.test.ts'],
       exclude: ['node_modules', 'dist'],
       setupFiles: ['./tests/setup.ts'],
       environment: 'node',
       globals: false,
       reporters: ['default'],
       coverage: {
         provider: 'v8',
         reporter: ['text', 'html'],
         exclude: ['dist', 'tests', 'node_modules', '**/*.d.ts']
       }
     }
   });
   ```
2. Creare `tests/setup.ts`:
   ```typescript
   // Vitest bootstrap. Aggiungere qui hooks globali (beforeAll/afterAll)
   // se necessario per i contract test in phase successive.
   ```
3. Creare `tests/smoke.test.ts`:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { z } from 'zod';

   describe('bridge smoke', () => {
     it('zod v4 is installed and usable', () => {
       const schema = z.object({ ok: z.literal(true) });
       expect(schema.parse({ ok: true })).toEqual({ ok: true });
     });

     it('zod v4 rejects invalid input (fail-loud contract)', () => {
       const schema = z.object({ ok: z.literal(true) });
       // safeParse invece di parse per verificare che NON rompe a runtime
       const result = schema.safeParse({ ok: false });
       expect(result.success).toBe(false);
     });
   });
   ```
4. `pnpm test` → 2 test verdi.

**Success criteria (binario):**
- [ ] `pnpm test` exit code 0
- [ ] Output vitest mostra `2 passed | 0 failed`
- [ ] `node_modules/zod/package.json` version >= `4.0.0`

**Rollback:** `rm -rf vitest.config.ts tests/` → stato pre-task.

**Commit atomico:** `chore(bridge): vitest setup + smoke test (zod v4 sanity)`

---

### Task 00-01.4: README skeleton (OBS-01, OBS-02, OBS-03)

**Obiettivo singolo:** aggiornare README esistente con le 3 sezioni obbligatorie (tabella contratti, how-to-add, breaking change policy) richieste dai requirement Phase 0. Il README esistente ha gia qualcosa — **questo task aggiorna, non sovrascrive incondizionato**.

**File toccati (assoluti):**
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/README.md` (aggiornato)

**Step concreti:**
1. `cat /Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/README.md` → leggere contenuto esistente
2. Aggiornare/creare README con struttura minimale:
   ```markdown
   # @x9-forge/contracts

   Single source of truth per i contratti TypeScript cross-repo tra
   **agent-x9** (runtime multi-agent) e **forge-v2** (control plane multi-tenant).

   ## Install

   ```bash
   pnpm add "@x9-forge/contracts@git+https://github.com/App-Templates/x9-forge-contract.git#<SHA>"
   ```

   Il `prepare` script buildera il package al momento dell'install.

   ## Dev locale (hot-reload)

   Nel `package.json` root del consumer (X9 o Forge), aggiungere (NON committare in main):
   ```json
   {
     "pnpm": {
       "overrides": {
         "@x9-forge/contracts": "link:../x9-forge-contract-bridge"
       }
     }
   }
   ```
   Poi `pnpm install`. Modifiche al bridge → consumer le vede al successivo `tsc --noEmit`.

   ## Contracts coverage (OBS-01)

   Tabella popolata incrementalmente phase-per-phase. Oggi (Phase 0): **vuota**.

   | Dominio | Contratto | File | Phase aggiunto |
   |---------|-----------|------|----------------|
   | — | — | — | Phase 1+ |

   ## How to add a new contract (OBS-02)

   1. Identifica il dominio (capability/agent/http/auth/vault/model-router)
   2. Crea `src/<dominio>/<nome>.ts` con Zod schema + `z.infer<typeof schema>`
   3. Esporta da `src/<dominio>/index.ts`
   4. Scrivi contract test in `tests/<dominio>/<nome>.test.ts` (schema + fixture valida + fixture invalida)
   5. `pnpm build && pnpm test` verdi
   6. Aggiorna la tabella qui sopra
   7. Bump SHA nei consumer atomicamente (entrambi X9 + Forge in un singolo PR coordinato)

   ## Breaking change policy (OBS-03)

   - **SHA-pinned** nei consumer (no semver tag mobile)
   - **Breaking change = bump SHA simultaneo nei 2 consumer, mai uno solo**
   - **Deprecation**: aggiungi `/** @deprecated use X instead */` JSDoc, mantieni export per >= 1 cycle
   - **Rimozione**: solo dopo che entrambi i consumer hanno zero reference all'API deprecata (`grep -r` verifica)
   - **Never force-push** sul main del bridge
   - **Mai ruotare SHA senza PR cross-repo coordinato**

   ## Architecture

   Vedi [.planning/research/ARCHITECTURE.md](.planning/research/ARCHITECTURE.md).

   ## Development

   - `pnpm install` — installa dependencies
   - `pnpm build` — builda `src/` → `dist/`
   - `pnpm test` — esegue vitest
   - `pnpm typecheck` — type-check senza emit
   - `pnpm lint` — ESLint su src + tests
   ```
3. Verificare: `grep -c "Contracts coverage" README.md` → 1, `grep -c "How to add" README.md` → 1, `grep -c "Breaking change policy" README.md` → 1.

**Success criteria (binario):**
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/README.md` contiene sezione `## Contracts coverage` (OBS-01)
- [ ] README contiene sezione `## How to add a new contract` (OBS-02)
- [ ] README contiene sezione `## Breaking change policy` (OBS-03)
- [ ] README cita pattern `pnpm.overrides` + `link:` per dev locale (BRDG-06 documentato)
- [ ] README cita `git+https` URL con SHA pinning (BRDG-01 documentato)

**Rollback:** `git checkout HEAD -- README.md` → versione pre-task.

**Commit atomico:** `docs(bridge): README skeleton + coverage + how-to-add + breaking change policy`

---

### Task 00-01.5: ESLint config + GitHub Actions CI

**Obiettivo singolo:** abilitare lint locale e CI su push/PR. Setup minimale (build + test + lint), no deploy, no release.

**File toccati (assoluti):**
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/eslint.config.mjs` (creato)
- `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/.github/workflows/ci.yml` (creato)

**Step concreti:**
1. Creare `eslint.config.mjs` (flat config ESLint 9):
   ```javascript
   import tseslint from '@typescript-eslint/eslint-plugin';
   import tsparser from '@typescript-eslint/parser';

   export default [
     {
       files: ['src/**/*.ts', 'tests/**/*.ts'],
       languageOptions: {
         parser: tsparser,
         parserOptions: {
           project: './tsconfig.json',
           ecmaVersion: 2023,
           sourceType: 'module'
         }
       },
       plugins: {
         '@typescript-eslint': tseslint
       },
       rules: {
         '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
         '@typescript-eslint/no-explicit-any': 'error',
         '@typescript-eslint/consistent-type-imports': 'error',
         'no-console': 'warn'
       }
     },
     {
       ignores: ['dist/', 'node_modules/', 'coverage/']
     }
   ];
   ```
2. Creare `.github/workflows/ci.yml`:
   ```yaml
   name: CI
   on:
     push:
       branches: [main]
     pull_request:
       branches: [main]

   jobs:
     test:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - uses: pnpm/action-setup@v4
           with:
             version: 10
         - uses: actions/setup-node@v4
           with:
             node-version: '22'
             cache: 'pnpm'
         - run: pnpm install --frozen-lockfile
         - run: pnpm lint
         - run: pnpm build
         - run: pnpm test
   ```
3. `pnpm lint` → zero errori locale.

**Success criteria (binario):**
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/eslint.config.mjs` esiste
- [ ] `/Users/admintemp/Downloads/Claude/x9-forge-contract-bridge/.github/workflows/ci.yml` esiste
- [ ] `pnpm lint` exit code 0
- [ ] Dopo push su GitHub, CI `CI` verde su commit (verifica via `gh run list -R App-Templates/x9-forge-contract`)

**Rollback:** `rm -rf eslint.config.mjs .github/`

**Commit atomico:** `ci(bridge): github actions (lint+build+test) + eslint flat config`

---

## Success Criteria (plan-level, binari)

- [ ] Repo `x9-forge-contract-bridge` ha struttura completa: `src/{index.ts, 6 sub-domini/index.ts}`, `tests/{setup.ts, smoke.test.ts}`, `package.json`, `tsconfig.json`, `vitest.config.ts`, `eslint.config.mjs`, `.github/workflows/ci.yml`, `README.md`, `.gitignore`, `.nvmrc`
- [ ] `pnpm install && pnpm build && pnpm test && pnpm lint` tutti verdi in sequenza
- [ ] `dist/` contiene 7 `index.js` + 7 `.d.ts` + declaration maps
- [ ] GitHub Actions CI verde su ultimo commit push
- [ ] README contiene le 3 sezioni OBS-01, OBS-02, OBS-03
- [ ] 5 commit atomici separati pushati su main

---

## Pitfall mapping

| Pitfall (PITFALLS.md) | Task | Come viene prevenuto |
|---|---|---|
| P-01 Compat shim loop | Task 00-01.2 | Bridge non importa MAI da X9/Forge. `src/` contiene solo placeholder `export {}` |
| P-03 Zod sync (v3 vs v4) | Task 00-01.1, 00-01.3 | Bridge locked su `zod@^4.3.6` (devDep) + `zod@^4.0.0` (peerDep). Smoke test verifica zod v4 API |
| P-14 `exactOptionalPropertyTypes` asimmetria | Task 00-01.2 | `tsconfig.json` ha `exactOptionalPropertyTypes: true` — allineato al piu strict (X9). Forge dovra adeguarsi (00-03) |
| P-12 Barrel file sprawl | Task 00-01.2 | Sub-path exports dichiarati upfront. `index.ts` root e volutamente minimale. Phase 1+ aggiungera re-export dai sub-domini |
| P-28 README drift | Task 00-01.4 | README ha tabella placeholder con istruzione "popolata phase-per-phase". Phase 1+ richiede update |

---

## Risk mitigation

| Risk | Probabilita | Impatto | Mitigazione |
|------|-------------|---------|-------------|
| `prepare` script fallisce in futuri consumer install | Bassa | Medio | Phase 0 non consuma bridge in produzione. Test validation defer a Phase 1. Nota commentata nel README |
| Versione TS 6.0.2 non ancora stabile (aprile 2026) | Bassa | Basso | Verificato da research (STACK.md §11, 2026-04-14 npm). Stessa versione usata da agent-x9 |
| Zod v4 ha bug non conosciuti | Bassa | Basso | Zod v4.3.6 usato da X9 in produzione dal 2026-03. Nessun issue noto |
| GitHub Actions CI fallisce per reason fuori bridge | Bassa | Basso | CI minimale (lint+build+test). Nessun deploy. Retry facile |

---

## Rollback path (plan-wide)

Se 00-01 fallisce irreversibilmente:

1. `cd /Users/admintemp/Downloads/Claude/x9-forge-contract-bridge`
2. `git log --oneline` → identifica il primo commit di 00-01
3. `git reset --hard <sha-precedente-00-01>` → torna a stato pre-plan (repo con solo `.planning/` + `README.md` originale)
4. `git push --force-with-lease origin main` — **attenzione**: questo richiede OK esplicito Stefano (feedback rule "commit senza chiedere" non copre force push)
5. In alternativa (safer): `git revert <sha-00-01-1>..<sha-00-01-5>` → crea 5 commit revert, pushati normal

**Nota rollback:** `--force-with-lease` preferito su `--force` (piu sicuro). Ma la regola di Stefano e: no force push senza conferma esplicita. Default → revert.

---

## Verification checklist pre-SUMMARY

- [ ] `pnpm install && pnpm build && pnpm test && pnpm lint` sequenza verde
- [ ] `ls dist/` mostra 7 sub-dir + index.js
- [ ] `cat README.md | grep -c "## Contracts coverage\|## How to add\|## Breaking change policy"` → 3
- [ ] GitHub Actions CI run verde (verifica: `gh run list -R App-Templates/x9-forge-contract -L 5`)
- [ ] 5 commit atomici pushati, ognuno singola responsabilita
- [ ] X9 e Forge NON sono stati toccati (verifica: `git -C /Users/admintemp/Downloads/Claude/agent-x9 log --since=<plan-start>` → zero commit)

---

*Plan scritto 2026-04-14*
